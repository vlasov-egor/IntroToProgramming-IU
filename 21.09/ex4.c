#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

const int INF = 100000000;

int input(int n)
{
    scanf("%d", n);
}

int main()
{
    int n = input(n);
    int step = 1;

    while (step < n)
    {
        
    }
}